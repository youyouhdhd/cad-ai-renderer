interface:
  display_name: "CAD AI Renderer"
  short_description: "Codex Skill for geometry-anchored CAD renders"
  default_prompt: "Use $cad-ai-renderer to turn this STEP/STP or mesh model into deterministic CAD evidence and geometry-anchored product-render candidates through the host image-generation capability."
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
