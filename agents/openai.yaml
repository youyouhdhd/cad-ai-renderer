interface:
  display_name: "CAD AI Renderer"
  short_description: "Frozen-contract CAD renders with exact pixel delivery"
  default_prompt: "Use $cad-ai-renderer to freeze this CAD render contract, generate geometry-anchored candidates and one final master through the official image tool, then enforce exact delivery pixels with native/upscaled reporting and final QC."
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
